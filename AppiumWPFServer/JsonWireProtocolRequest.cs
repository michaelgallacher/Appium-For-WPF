using System;

namespace AppiumWpfServer
{
    public class JsonWireProtocolRequest
    {/*
        private readonly Lazy<dynamic> _lazyData;

        public JsonWireProtocolRequest()
        {
            _lazyData = new Lazy<dynamic>(() => JObject.Parse(Json));
        }

        public string Json { get; set; }

        public dynamic Data
        {
            get { return _lazyData.Value; }
        }
    */
    }
}